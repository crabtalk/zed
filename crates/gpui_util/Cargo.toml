[package]
name = "bezel-gpui-util"
version = "0.3.1+zed.d9ad6a"
description = "gpui_util from zed-industries/zed, republished for the bezel gpui fork"
license = "Apache-2.0"
readme = "README.md"
repository = "https://github.com/crabtalk/zed"
edition.workspace = true

[lib]
name = "gpui_util"

[dependencies]
log.workspace = true
anyhow.workspace = true

[target.'cfg(target_os = "windows")'.dependencies]
which.workspace = true

[lints]
workspace = true
