[package]
name = "bezel-zed-http-client-tls"
version = "0.3.1+zed.d9ad6a"
description = "http_client_tls from zed-industries/zed, republished for the bezel gpui fork"
readme = "README.md"
repository = "https://github.com/crabtalk/zed"
edition.workspace = true
license = "Apache-2.0"

[lints]
workspace = true

[features]
test-support = []

[lib]
name = "http_client_tls"
path = "src/http_client_tls.rs"
doctest = true

[dependencies]
rustls.workspace = true
rustls-platform-verifier.workspace = true
