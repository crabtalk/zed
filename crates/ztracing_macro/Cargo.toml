[package]
name = "bezel-zed-ztracing-macro"
version = "0.3.3+zed.d9ad6a"
description = "ztracing_macro from zed-industries/zed, republished for the bezel gpui fork"
readme = "README.md"
repository = "https://github.com/crabtalk/zed"
edition.workspace = true
license = "GPL-3.0-or-later"

[lib]
name = "ztracing_macro"
proc-macro = true

[dependencies]
